# Core
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0           # LoRA support (only needed when use_lora=True)

# Data
pyyaml>=6.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Optional – accelerate makes Qwen loading faster
# accelerate>=0.27.0
